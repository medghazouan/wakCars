# Optional. Leave unset in dev: the marketing site calls /api/public/* on the Vite origin;
# Vite proxies /api to Express (see vite.config.js).
# Production: API origin only (no /api path). Client builds URLs as {VITE_API_URL}/api/public/...
# VITE_API_URL=https://api.yourdomain.com
